
<dialog id="dialog">
    <form id="editForm" action="/goAnnotation" method="post">
    <h2 class = "h2Modal">커스텀 모델 만들기</h2>
    <p class="pModal">자동라벨링을 원하시는 이미지를 예측하기 위해서는 이미지의 학습이 필요합니다.</p>
    <p class="pModal">학습시킬 이미지는 10% 이상을 권장합니다.</p>
    <!-- 범위 슬라이더  넣기 -->
    <div class="wrap">
        <input type="range" name="slide" value="10" step="1" min="1" max="100"/>
        <p class="pNumbers">10 %</p>
    </div>
    <p class="pSummary" style="text-align: right;font-size: x-large;"> 전체 이미지의 <span class="pNumbers" style="text-align: right;font-size: x-large;font-weight: bolder;"></span> : <br>
    <span class = "pNumbers2" style="text-align: right;font-size: xx-large;font-weight: bolder;"></span>
    </p>
    <br>
    <p class="pModal">진행하기를 누르시면 전체 이미지의 <span class="pNumbers2"></span>을 수동으로 진행합니다.</p>
    <input type="hidden" name="projectIdx" value="{{projectIdx}}">
        <input id="labelFolder" name="labelFolder" type="hidden">
    <button type="submit" class="btn mt-4" id="btnDialog" onclick="submitForm()">진행하기</button>
    <button aria-label="close" class="x" id="closeModal">❌</button>
    </form>

</dialog>
